///@desc Will return a client instance with a client id matchign the given argument
///@param {Integer} client_id the client id to searc for

var _client_id = argument0

with (o_client){
	if (m_client_id = _client_id) return id
}

return noone